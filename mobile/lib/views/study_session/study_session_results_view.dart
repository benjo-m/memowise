import 'package:flutter/material.dart';

class StudySessionResultsView extends StatelessWidget {
  const StudySessionResultsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Session results"),
        centerTitle: true,
      ),
    );
  }
}
