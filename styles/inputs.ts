import { StyleSheet } from "react-native";

export const inputStyles = StyleSheet.create({
  base: {
    backgroundColor: "#f0f0f0",
    padding: 12,
    borderRadius: 10,
    fontSize: 16,
    borderWidth: 1,
    borderColor: "#ccc",
    width: "90%",
  },
});
