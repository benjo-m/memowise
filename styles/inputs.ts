import { StyleSheet } from "react-native";

export const inputStyles = StyleSheet.create({
  base: {
    backgroundColor: "#fff",
    padding: 12,
    borderRadius: 10,
    fontSize: 16,
    borderColor: "#ccc",
    width: "100%",
  },
});
