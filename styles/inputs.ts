import { StyleSheet } from "react-native";

export const inputStyles = StyleSheet.create({
  base: {
    backgroundColor: "#fff",
    padding: 12,
    borderRadius: 10,
    fontSize: 16,
    borderWidth: 2,
    borderColor: "#ccc",
    width: "100%",
  },
});
